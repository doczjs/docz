---
import { store } from "../hooks/useStore";

const { of } = Astro.props;
const { docgenInfo } = of.__filemeta;
const { components = {} } = store.get();
const Props = components?.Props;
---

{docgenInfo?.props && <Props info={docgenInfo.props} />}
