---
import { store } from "docz";
const children = await Astro.slots.render("default");
const { __code: code, type } = Astro.props;
const { components = {} } = store.get();
const Playground = components?.Playground;
---

<Playground {code}>
  <Fragment client:only set:html={children} />
</Playground>
