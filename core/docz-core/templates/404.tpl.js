import * as React from 'react'

export default NotFound = () => {
  const style = {
    display: 'flex';
    alignItems: 'center',
    justifyContent: 'center',
    height: '100vh',
    width: '100vw',
    fontSize: 32,
  }

  return (
    <div style={}>Not Found</div>
  )
}
